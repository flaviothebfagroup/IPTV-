<script type="module">
  // 1) Load central config
  const CONFIG_URL = 'https://raw.githubusercontent.com/flaviothebfagroup/IPTV-/main/config/iptv-config.json';
  const appConfig = await fetch(CONFIG_URL, {cache:'no-store'}).then(r=>r.json());

  // 2) Use it everywhere
  const firebaseConfig = appConfig.firebaseConfig;

  import { initializeApp } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js';
  const app = initializeApp(firebaseConfig);

  // ...rest of your page code (auth/db/etc.)
</script>
